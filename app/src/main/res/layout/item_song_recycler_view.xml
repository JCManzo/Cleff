<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    android:id="@+id/song_layout_container"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="?android:listPreferredItemHeight"
    android:background="@drawable/song_list_selected_item"
    android:padding="5dp"
    android:paddingLeft="10dp"
    android:layout_gravity="center_vertical" >

    <TextView
        android:id="@+id/recyler_view_song_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:singleLine="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:ellipsize="marquee"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/recycler_view_song_artist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:singleLine="true"
        android:layout_below="@id/recyler_view_song_title"
        android:textColor="@color/text_secondary"
        android:textSize="18sp" />
</RelativeLayout>
